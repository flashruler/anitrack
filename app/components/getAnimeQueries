export const fetchData = async(props)=>{
    const [seasonRes] = await Promise.all([
        fetch(`https://api.jikan.moe/v4/anime?q=`+props), 
      ]);
      const [season] = await Promise.all([
        seasonRes.json(),
      ]);
      return season;
}